# CLAUDE.md — project-safehome-ai-api

**이 저장소에서의 역할과, 관행과 달라 모르면 틀리는 것들.**
관행대로 쓴 코드는 문법도 구조도 멀쩡하다. 그래서 어기면 대개 에러 없이 잘못 동작한다.
스택·구조·실행 명령·응답 스키마·판정 규칙의 구체적인 내용 → [`README.md`](README.md)

---

## 역할과 경계

AI 분석 서버다. 입력을 받아 구조화된 분석 결과를 돌려주는 것이 전부다.

**여기서 하지 않는 일**

- **상태를 갖지 않는다.** 저장하지 않고, 요청 간에 기억하지 않는다. 이력 저장은 백엔드 소관이다.
  (지식 저장소는 읽기 전용 자원이지 애플리케이션 상태가 아니다.)
- **사용자 인증을 하지 않는다.** 백엔드에서 오는 내부 호출만 받는다.
- **클라이언트가 직접 부르는 서버가 아니다.** 앱이 이 서버를 호출하는 구조를 전제하지 않는다.
- **파일 파싱을 하지 않는다.** 정제된 텍스트를 받는다.

**혼자 정하지 않는 것**

- 응답 필드의 제거·개명 (계약이다)
- 판정 결과를 바꾸는 기준 변경 — 같은 입력의 답이 달라진다
- 내부 계산용 데이터를 응답에 노출하는 것

## 시작 전

1. [`README.md`](README.md) — **설계 원칙(역할 분리)** · **작업 레시피** · **API→AI API 계약**
2. 검색 모듈과 지식 데이터의 각 `README.md`

## 필수 절차

- **응답·문서·주석은 한국어로 쓴다.** 코드 식별자는 영문.
- **판정은 결정적 코드가, 서술은 모델이 한다.** 등급·상태 판정을 모델에게 맡기지 않는다.
  맡기면 같은 입력에 매번 다른 등급이 나온다. 이 분리를 택한 근거는 README의 결정 이유 절에 있다.
- **프롬프트나 판정 로직을 바꾸면 호출하는 쪽의 캐시를 무효화해야 한다.**
  캐시 키의 버전을 올리지 않으면 옛 결과가 만료될 때까지 계속 나간다.
- **판정에 쓰이는 분류값의 허용 범위를 바꿀 때는 프롬프트와 판정 코드를 함께 고친다.**
  판정이 문자열 대조라, 한쪽만 고치면 **에러 없이 안전한 쪽으로 떨어진다.**
  판정 테스트는 대조 규칙 자체는 고정하지만, 프롬프트와의 정합은 보지 않는다.
- **응답 필드를 추가하면** 응답 생성부 → 이 저장소 README의 **API→AI API 계약** 절 → 하류 순으로 전파한다.
- **지식 데이터를 추가하면 로더의 목록에 등록하고 벡터 저장소를 다시 만든다.**
  등록하지 않으면 **경고도 에러도 없이 무시된다.** 저장소는 비어 있을 때만 채워진다.
- **테스트를 추가하거나 지우면 문서의 "무엇이 잡히는가" 서술을 함께 본다.**
  이 저장소의 문서는 어겼을 때 신호가 나는지 여부를 적는다. 방어 장치가 바뀌면
  그 서술이 조용히 거짓이 된다 — 코드가 아니라 검증 체계가 바뀌어서 생기는 드리프트다.
  제약 테스트를 만들면 **실패 메시지에** 이유와 근거 문서를 적고, **검사할 대상을 하나도 찾지 못하면 실패하게** 만든다.
  근거 문서가 없으면 어긴 순간에 문서가 도착하지 않고, 대상이 비면 아무것도 보지 않은 채 통과한다.
- **코드를 고치면 가장 가까운 `README.md` 를 함께 본다.** 구조·관통 흐름·불변식·실패 규칙·설계 결정이
  바뀌었으면 같은 커밋에서 고친다. 이름만 바꿨거나 내부 구현만 손봤다면 그대로 둔다.
  문서의 형식은 제약 테스트가 지킨다 — 링크와 인용한 절이 실제로 있는지, 계약 절의 표식,
  머리글과 문서 지도, 제약 테스트의 근거 문서. **문장이 코드 동작에 대해 사실인지는 테스트가 보지 못한다.**
  그것이 이 절차의 몫이다.
- **끝났다고 보고하기 전에 `scripts/verify.sh` 를 돌린다.** CI 의 테스트 단계가 같은 파일을 실행한다 —
  명령이 다르면 로컬 통과가 CI 통과를 뜻하지 않는다. 푸시 뒤에는 `scripts/ci_status.py` 로 원격 결과를 본다.
  보고는 **로컬 검증 / 원격 결과 / 확인하지 못한 것**으로 나눈다. 실행해 보지 않은 동작은 "확인 안 됨"이다.
- **앱이 불러오는 로컬 모듈은 이미지 정의에도 넣는다.** 빠뜨리면 테스트는 통과하고 컨테이너만 뜨지 못한다.
  제약 테스트가 검사하고, CI 가 이미지를 올리기 전에 실제로 띄워 본다.

## 금지사항

- **분석 호출의 무작위성 설정을 바꾸지 않는다.** 같은 입력에 같은 결과가 나와야 한다.
- **모델 출력을 자유 텍스트로 받아 긁어내지 않는다.** 구조화 출력을 강제하고 파싱한다.
  파싱 실패는 조용히 넘기지 말고 오류로 다룬다.
- **검색(RAG) 실패를 치명적으로 처리하지 않는다.** 예외는 잡아 로그만 남기고
  모델 단독 분석으로 진행한다.
- **내부 계산용 필드를 응답에 넣지 않는다.** 노출이 필요하면 계약부터 합의한다.
- **워커 프로세스를 늘리지 않는다.** 벡터 저장소가 프로세스마다 저장 파일을 잡아
  둘 이상이면 충돌한다. 동시 처리량이 필요하면 다른 방법을 찾는다.
