# CLAUDE.md — project-safehome-ai-api

**이 저장소에서의 역할과, 어겼을 때 조용히 깨지는 것들.**
스택·구조·실행 명령·응답 스키마·판정 규칙의 구체적인 내용 → [`README.md`](README.md)

---

## 역할과 경계

AI 분석 서버다. 입력을 받아 구조화된 분석 결과를 돌려주는 것이 전부다.

**여기서 하지 않는 일**

- **상태를 갖지 않는다.** 저장하지 않고, 요청 간에 기억하지 않는다. 이력 저장은 백엔드 소관이다.
  (지식 저장소는 읽기 전용 자원이지 애플리케이션 상태가 아니다.)
- **사용자 인증을 하지 않는다.** 백엔드에서 오는 내부 호출만 받는다.
- **클라이언트가 직접 부르는 서버가 아니다.** 앱이 이 서버를 호출하는 구조를 전제하지 않는다.
- **파일 파싱을 하지 않는다.** 정제된 텍스트를 받는다.

**혼자 정하지 않는 것**

- 응답 필드의 제거·개명 (계약이다)
- 판정 결과를 바꾸는 기준 변경 — 같은 입력의 답이 달라진다
- 내부 계산용 데이터를 응답에 노출하는 것

## 시작 전

1. [`README.md`](README.md) — **설계 원칙(역할 분리)** · **작업 레시피** · **응답 계약**
2. 검색 모듈과 지식 데이터의 각 `README.md`

## 필수 절차

- **판정은 결정적 코드가, 서술은 모델이 한다.** 등급·상태 판정을 모델에게 맡기지 않는다.
  **재현성이 상류 캐시 전략의 전제**라, 흔들리면 캐시된 옛 결과와 새 결과가 섞인다.
- **프롬프트나 판정 로직을 바꾸면 호출하는 쪽의 캐시를 무효화해야 한다.**
  캐시 키의 버전을 올리지 않으면 옛 결과가 만료될 때까지 계속 나간다.
- **판정에 쓰이는 분류값의 허용 범위를 바꿀 때는 프롬프트와 판정 코드를 함께 고친다.**
  판정이 문자열 대조라, 한쪽만 고치면 **에러 없이 안전한 쪽으로 떨어진다.**
- **응답 필드를 추가하면** 응답 생성부 → 이 저장소 README의 계약 절 → 하류 순으로 전파한다.
- **지식 데이터를 추가하면 로더의 목록에 등록하고 벡터 저장소를 다시 만든다.**
  등록하지 않으면 **경고도 에러도 없이 무시된다.** 저장소는 비어 있을 때만 채워진다.

## 금지사항

- **분석 호출의 무작위성 설정을 바꾸지 않는다.** 같은 입력에 같은 결과가 나와야 한다.
- **모델 출력을 자유 텍스트로 받아 긁어내지 않는다.** 구조화 출력을 강제하고 파싱한다.
  파싱 실패는 조용히 넘기지 말고 오류로 다룬다.
- **검색(RAG) 실패를 치명적으로 처리하지 않는다.** 예외는 잡아 로그만 남기고
  모델 단독 분석으로 진행한다. 검색은 품질 보조이지 필수 경로가 아니다.
- **내부 계산용 필드를 응답에 넣지 않는다.** 노출이 필요하면 계약부터 합의한다.
- **워커 프로세스를 늘리지 않는다.** 벡터 저장소가 프로세스마다 저장 파일을 잡아
  둘 이상이면 충돌한다. 동시 처리량이 필요하면 다른 방법을 찾는다.
- **자동 생성되는 벡터 저장소를 커밋하지 않는다.**
- **비밀값을 코드·문서에 하드코딩하지 않는다.** 환경 변수로만 받는다.
